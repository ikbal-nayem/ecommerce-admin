import WxNotFound from "@components/NotFound/NotFound";

const PageNotFound = () => {
	return (
		<WxNotFound
			title="Page not found"
			description="The page you are looking for is not exist."
		/>
	);
};

export default PageNotFound;
